{
  "name": "railmadad-ai-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "type-check": "npm run type-check --prefix frontend"
  }
}
